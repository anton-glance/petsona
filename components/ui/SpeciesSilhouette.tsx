// Skeleton — red phase. Implementation lands in the next commit.
import * as React from 'react';
import { View } from 'react-native';

export interface SpeciesSilhouetteProps {
  species: 'cat' | 'dog';
  tint?: string;
  testID?: string;
}

export function SpeciesSilhouette(_props: SpeciesSilhouetteProps): React.JSX.Element {
  return <View />;
}

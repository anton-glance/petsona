// Skeleton — red phase. Implementation lands in the next commit.
import * as React from 'react';
import { Text, View } from 'react-native';

export default function PhotoCollection(): React.JSX.Element {
  return (
    <View>
      <Text>placeholder</Text>
    </View>
  );
}
